apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resultappchart.fullname" . }}-test-connection"
  labels:
    {{- include "resultappchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resultappchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
