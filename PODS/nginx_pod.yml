apiVersion: v1
kind: Pod
metadata:
  name: pod-nginx
  labels:
    app: nginx
    tier: frontend
spec:
  containers:
  -  name: nginx
     image: nginx
     #docker hub image name

# kubectl get pods --all-namespaces
# kubectl delete --all pods --namespace=default


